function Account(props) {
    const balance = props.balance; 
    return (
        <div>
          <p>THIS MESSAGE IS FROM ACCOUNT AVALABLE BALANCE : {balance}</p>
        </div>
      );

}
export default Account